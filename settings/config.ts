// Настройки скрипта

export const config = {
  // Автозапуск
  autoStart: true,
  timeouts: {
    // Задержка после запуска системы
    systemStart: 5,
    // Задержка после запуска каждой копии webisida
    webisidaStart: 30,
  },
};
